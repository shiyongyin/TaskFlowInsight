name: tfi-flow-spring-starter CI

on:
  push:
    branches: [ main, 'feature/**' ]
    paths:
      - 'tfi-flow-spring-starter/**'
      - '.mvn/static-analysis-baseline.json'
      - 'config/pmd/**'
      - 'scripts/enforce_static_analysis_baseline.py'
      - 'pom.xml'
      - '.github/workflows/tfi-flow-spring-starter-ci.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'tfi-flow-spring-starter/**'
      - '.mvn/static-analysis-baseline.json'
      - 'config/pmd/**'
      - 'scripts/enforce_static_analysis_baseline.py'
      - 'pom.xml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test (Java ${{ matrix.java }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '21' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Install dependency modules
        run: ./mvnw install -pl tfi-flow-core -DskipTests -q

      - name: Run unit tests + JaCoCo
        run: ./mvnw verify jacoco:report -pl tfi-flow-spring-starter -q

      - name: Upload JaCoCo report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: tfi-flow-spring-starter-jacoco-java${{ matrix.java }}
          path: tfi-flow-spring-starter/target/site/jacoco/
          retention-days: 14

  static-analysis:
    name: Static Analysis
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Install dependency modules
        run: ./mvnw install -pl tfi-flow-core -DskipTests -q

      - name: SpotBugs
        run: ./mvnw compile spotbugs:check -pl tfi-flow-spring-starter -q

      - name: Generate Checkstyle and PMD reports
        run: >-
          ./mvnw checkstyle:check pmd:check -pl tfi-flow-spring-starter -q
          -Dcheckstyle.consoleOutput=false -Dpmd.printFailingErrors=false

      - name: Enforce static analysis baseline
        run: >-
          python3 scripts/enforce_static_analysis_baseline.py
          --module tfi-flow-spring-starter

      - name: Upload static analysis reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: tfi-flow-spring-starter-static-analysis-java21
          path: |
            tfi-flow-spring-starter/target/spotbugsXml.xml
            tfi-flow-spring-starter/target/checkstyle-result.xml
            tfi-flow-spring-starter/target/pmd.xml
          if-no-files-found: error
          retention-days: 14
